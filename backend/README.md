# TCM Backend

Express API with MongoDB.

## Commands

```bash
npm run dev:backend
npm run seed
```

## Local MongoDB

Install MongoDB Community Server, then start the MongoDB service from Windows Services or run `mongod`.

Default connection:

```bash
mongodb://127.0.0.1:27017/tcm_ac
```
